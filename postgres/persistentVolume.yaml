apiVersion: v1
kind: PersistentVolume
metadata:
  name: psql
  namespace: psql
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 500m
  hostPath:
    path: /data/psql/